## Policy Guardian v1.0.0

This folder is the v1.0.0 release source/binaries bundle.

For the SHA-256 of the surrounding ZIP artifact, use the value provided alongside the release download (or compute with `Get-FileHash`).

### Gates (Windows PowerShell)

```powershell
go clean
go build ./cmd/...
go test ./...
```
